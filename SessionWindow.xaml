<Window x:Class="ConnectionApp.SessionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConnectionApp"
        mc:Ignorable="d"
        Title="SessionWindow" Height="300" Width="400">
    <Grid>
        <DataGrid x:Name="SessionDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="User" Binding="{Binding User}" Width="*" />
                <DataGridTextColumn Header="Session ID" Binding="{Binding SessionId}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
